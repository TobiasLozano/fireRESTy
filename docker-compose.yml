services:
  fireresty:
    container_name: fireresty
    image: tobiaslozano/fireresty:latest
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URL=mongodb+srv://venttys:s5CReUCTug@cluster0.sqyyu78.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0/test
    volumes:
      - /path/to/storage:/data
    restart: unless-stopped

volumes:
  mongo_data:
